Continue NONDESTRUCTIVELY from the existing Core Platform codebase and preserve all current working functionality.

Now implement the final approval/denial logic, subscription gating, and public directory eligibility rules for the provider application system.

Business rules:
- registering an account does NOT make someone publicly listed
- submitting an application does NOT make someone publicly listed
- recurring membership subscription should only be available AFTER approval
- after approval, provider should see “Approved — Activate Membership”
- after recurring membership becomes active, provider can complete their public listing/profile
- provider should only be eligible for directory listing when:
  1. application approved
  2. recurring membership active
  3. required public profile/listing fields completed

Build the following:

A. Approval behavior
When admin approves:
- application decision becomes approved
- overall provider status updates to approved_pending_subscription
- provider dashboard updates to “Approved — Activate Membership”
- subscription activation CTA becomes available
- timeline event is created

B. Denial behavior
When admin denies:
- application decision becomes denied
- provider dashboard reflects denial appropriately
- application remains auditable
- add refund eligibility tracking for the stated $100 refund logic
- timeline event is created

C. Subscription gating
Update all relevant dashboard/subscription logic so recurring membership activation is only possible after approval.
Do not break the existing billing system; gate access to it correctly for this provider application workflow.

D. Directory gating / eligibility
Ensure public directory listing eligibility requires:
- approved application
- active membership subscription
- completed required listing/profile data

E. Dashboard final states
Support and render at least:
- Registered
- Application In Progress
- Application Submitted
- Under Review
- Approved — Activate Membership
- Active Member
- Denied

F. Final cleanup / QA pass
Do a focused cleanup for:
- status consistency
- route wiring
- error handling
- loading states
- type safety
- dead code removal
- copy consistency
- mobile responsiveness
- audit timeline completeness

G. Developer notes
If the app has an internal docs/dev docs area, add a concise note describing this provider application workflow and its key statuses.

Implementation rules:
- remain NONDESTRUCTIVE
- preserve existing subscription/payment flows
- keep approval logic centralized and consistent
- no hardcoded secrets
- do not break public directory behavior

At the end, provide:
1. What approval/denial logic now exists
2. How subscription gating works now
3. How directory eligibility is enforced
4. A concise QA checklist for testing the full end-to-end flow
5. Any recommended phase-2 improvements