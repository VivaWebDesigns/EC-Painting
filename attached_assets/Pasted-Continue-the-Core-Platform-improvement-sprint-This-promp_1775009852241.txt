Continue the Core Platform improvement sprint.

This prompt is focused ONLY on database performance and relational integrity.

Goal:
Add the most valuable indexes and foreign key constraints for query-heavy tables, with safe migration planning and no destructive data loss.

Tasks:
1. Review current Drizzle schema definitions and identify missing:
   - indexes
   - foreign keys
   - relational constraints
2. Implement indexes for query-heavy patterns, especially around:
   - therapist_profiles(is_approved, is_active, practice_mode, accepting_clients)
   - notifications(user_id, is_read, created_at)
   - direct_messages(conversation_id, created_at)
   - direct_messages(sender_id, created_at)
3. If specializations and languages are stored as arrays, evaluate and implement the best indexing strategy supported by the current DB approach, such as GIN indexes where appropriate.
4. Add FK constraints where appropriate, especially:
   - notifications.user_id -> users.id
   - any other clearly orphan-prone relations you find
5. Create safe migrations for all schema updates.
6. Add migration notes covering:
   - backward compatibility concerns
   - handling existing orphaned rows if present
   - recommended rollout order in production
7. If existing bad data would block FK creation, implement a safe remediation strategy:
   - identify orphans
   - document cleanup
   - avoid destructive deletion unless explicitly justified

Documentation:
- Add an ADR or architecture note explaining:
   - why indexes were chosen
   - how query patterns map to indexes
   - any future indexing suggestions for search and analytics

Rules:
- NON-DESTRUCTIVE
- Do not rename major schema fields unless absolutely necessary
- Do not introduce speculative indexes without explaining query value
- Prefer indexes backed by real application query paths
- Keep Drizzle schema definitions clean and readable

Definition of done:
- indexes added for high-value query paths
- foreign keys added where safe
- migrations created
- data integrity risks documented