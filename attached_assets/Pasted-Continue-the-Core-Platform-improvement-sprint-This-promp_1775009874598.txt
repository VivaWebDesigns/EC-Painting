Continue the Core Platform improvement sprint.

This prompt is focused ONLY on security hardening.

Goal:
Harden the authentication and mutation surface without breaking legitimate application use.

Tasks:
1. Audit current secret/session handling.
2. Remove insecure production fallback behavior:
   - In non-development environments, the app must fail fast if SESSION_SECRET or equivalent secret is missing
   - Development may still use a local fallback if appropriate
3. Add rate limiting / brute-force protection to sensitive endpoints, especially:
   - login
   - password reset
   - any magic-link or auth verification routes if present
4. Add CSRF protections for cookie-authenticated mutation routes.
   Choose the least disruptive secure approach based on the current architecture, such as:
   - strict origin checking for same-site app flows
   - CSRF token strategy where needed
5. Add security middleware such as helmet, configured appropriately for this stack.
6. Add request payload size limits where appropriate.
7. Review cookie settings and keep or improve:
   - httpOnly
   - sameSite
   - secure in production
8. Add security-focused error handling that does not leak implementation details.
9. Document all new security assumptions and environment requirements.

Also:
- Review admin and auth endpoints for missing authorization guards
- Ensure the public directory/search routes remain accessible as intended
- Preserve developer ergonomics in local dev

Documentation:
- Add /docs/runbooks/security.md or similar
- Include:
   - required environment variables
   - rate limit strategy
   - CSRF strategy
   - secure deployment notes

Rules:
- NON-DESTRUCTIVE
- Do not break current session-based auth flows
- Avoid adding security controls that would block normal frontend operations
- Any new middleware must be scoped and tested carefully

Definition of done:
- production secret fallback removed
- rate limiting present on auth-sensitive endpoints
- CSRF defense present for state-changing cookie-auth routes
- security headers added
- docs updated