Perform a full nondestructive end-of-day quality pass on the Core Platform CMS > SEO implementation.

GOALS
- reduce code debt introduced during the SEO sprint
- catch regressions
- improve reliability
- confirm structured data output is correct and tied to visible content
- refresh documentation thoroughly

IMPORTANT GLOBAL REQUIREMENTS
1. Ensure all SEO/social/structured-data-related image inputs use Cloudflare R2-backed upload/dropzone or media selection flows instead of raw pasted image URLs.
2. Ensure structured data is emitted as JSON-LD and only for content types/pages where it truthfully matches the visible public page content.

REQUIREMENTS
1. Run full validation:
   - typecheck/check
   - build
   - lint if configured
   - tests if configured

2. Fix any regressions introduced during the SEO sprint.

3. Clean up low-risk technical debt directly related to SEO/structured-data work.

4. Review these key areas:
   - CMS > SEO global settings
   - page SEO editing
   - blog SEO editing
   - SEO image uploads
   - metadata rendering
   - Organization JSON-LD
   - Breadcrumb JSON-LD
   - Article JSON-LD
   - Event JSON-LD
   - VideoObject JSON-LD where applicable
   - FAQPage JSON-LD where applicable
   - redirects
   - sitemap visibility
   - robots/noindex visibility
   - SEO audit tables
   - preview and validation UX

5. Confirm there are no remaining raw image URL fields in CMS > SEO where upload/media selection should exist.

6. Confirm structured data output matches visible content and does not include misleading or inapplicable schema types.

7. Update docs comprehensively.

IMPORTANT RULES
- Nondestructive only
- No broad refactors unrelated to recent SEO work
- No feature removals
- No accidental public metadata regressions
- No speculative cleanup outside current scope

REQUIRED OUTPUT
Provide:
1. Validation results
2. Bugs fixed
3. Code debt cleaned up
4. Performance or reliability improvements made
5. Confirmation of R2-backed SEO image handling
6. Confirmation of truthful JSON-LD structured data output
7. Remaining risks or follow-up recommendations
8. Docs updated