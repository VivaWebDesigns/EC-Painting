You are working inside the existing Core Platform codebase.

This is a NONDESTRUCTIVE implementation task. Do not remove or break any existing auth, dashboard, profile, subscription, admin, or directory functionality. Reuse existing patterns wherever possible. Preserve all current working features. Build incrementally.

We need to begin implementing the mental health professional membership application workflow that starts when a logged-in provider clicks the existing dashboard button:

“Apply for Membership and Get Listed”

Before building, inspect the codebase and identify:
1. How provider registration currently works
2. How provider dashboard status is currently rendered
3. How profile completion is currently tracked
4. How subscription state is currently managed
5. Existing route patterns for dashboard/admin areas
6. Existing DB/schema structure for users/providers/subscriptions/profiles
7. Existing email and payment infrastructure
8. Existing admin review/list/detail conventions

Then implement the NONDESTRUCTIVE foundation for the new application system:

A. Add a provider application architecture plan to the codebase
- Create or update schema definitions and types for a new provider application domain
- Add a formal application status model
- Add sub-status support for:
  - payment_status
  - references_status
  - background_check_status
  - interview_status
  - decision_status

B. Add the minimum database scaffolding needed for:
- provider_applications
- provider_application_timeline
- provider_application_credentials
- provider_application_references
- provider_background_checks
- provider_interviews
- provider_application_decisions

C. Add safe application state enums similar to:
- not_started
- draft
- submitted
- awaiting_background_check
- background_check_in_progress
- awaiting_references
- references_in_progress
- ready_for_interview
- interview_scheduled
- interview_completed
- approved_pending_subscription
- active_member
- denied
- withdrawn

D. Update the provider dashboard routing behavior so the existing button:
“Apply for Membership and Get Listed”
routes into a new provider application area instead of behaving like a subscription checkout action.

Suggested route:
- /dashboard/application
or the best existing route convention in the app

E. Add placeholder route/page scaffolding for:
- provider application wizard
- provider application status page
- admin application list
- admin application detail

F. Update provider dashboard status card logic NONDESTRUCTIVELY to support future states:
- Registered
- Application In Progress
- Application Submitted
- Approved — Activate Membership
- Active Member

For this prompt, do not build the whole form yet.
Focus on:
- codebase inspection
- schema/migration scaffolding
- route scaffolding
- status model
- clean service/domain organization
- safe dashboard entrypoint update

Implementation rules:
- no giant god files
- use modular routes/services/components
- use strong typing
- preserve all existing behavior unless intentionally extending it
- do not break the current subscription system; only gate it for this new provider approval flow
- do not hardcode any secrets or credentials
- if a third-party integration is not yet implemented, scaffold it cleanly

At the end, provide:
1. What existing structures you found
2. What new schema/migrations you added
3. What routes/pages/services you created
4. Any assumptions you made
5. Any risks or follow-up items before Prompt 2